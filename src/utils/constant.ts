export const LOCAL_TOKEN_KEY = 'uni_id_token'
export const LOCAL_TOKEN_EXPIRED_KEY = 'uni_id_token_expired'

// 单位ms，token有效期小于tokenExpiresThreshold则重新获取
export const LOCAL_TOKEN_EXPIREDS_THRESHOLD = 120 * 60 * 1000

/* 默认 全部tag的ID标识 */
export const DEFAULT_TAG_ID = '-1'

/* 归档tag的ID标识 */
export const STORE_TAG_ID = '-2'

/* 默认分页查询每页的数据量 */
export const PAGE_SIZE = 20

/* 上传图片的最大图片数目 */
export const IMAGE_LIST_LENGTH = 9
